import { LoadingCard } from "@/components/LoadingCard";
import { PageContainer } from "@/components/PageContainer";

export default function Loading() {
  return (
    <PageContainer>
      <LoadingCard title="Loading AdmitWise" />
    </PageContainer>
  );
}
